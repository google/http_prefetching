## HTTP prefetching experiments
**Disclaimer: This is not an official Google product.**

This implements a proxy that injects prefetch hints, and a browser extension
that prefetches pages based on received hints.
